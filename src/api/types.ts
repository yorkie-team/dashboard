export type DocumentSummary = {
  id: string;
  key: string;
  snapshot: string;
};

export type ProjectSummary = {
  id: string;
  name: string;
  publicKey: string;
  secretKey: string;
  createdAt: string;
};
